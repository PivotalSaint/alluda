const router = require('express').Router();

const { route } = require('../../controllers/apiRoutes/user-routes')
const userRoutes = require('../../controllers/apiRoutes/user-routes');

// router.use('/users', userRoutes);

module.exports = router;